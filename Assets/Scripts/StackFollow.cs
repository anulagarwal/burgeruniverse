using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StackFollow : MonoBehaviour
{
    //private Transform follow;
    //private float followSpeed = 100f;

    //void Start()
    //{
    //    follow = transform.parent.GetChild(transform.GetSiblingIndex() - 1);
    //}

    //void LateUpdate()
    //{
    //    transform.position = new Vector3(Mathf.Lerp(transform.position.x, follow.position.x, followSpeed * Time.deltaTime),
    //         transform.position.y,
    //         Mathf.Lerp(transform.position.z, follow.position.z, followSpeed * Time.deltaTime));
    //}
}
